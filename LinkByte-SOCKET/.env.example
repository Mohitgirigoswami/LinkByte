# LinkByte WebSocket Server Configuration
# ---------------------------------------
# These variables are required to run the Socket.IO server.
# Copy this file to .env and fill in the values.

# The same JWT secret key used in the backend to verify the authenticity of connecting clients.
SECRET_KEY="your_strong_jwt_secret_key"

# The URL of the backend API, which the socket server calls to persist messages.
BEURL="http://localhost:5000"
